import { user } from './user';
// import { JsonObjectType, JsonValueType } from '../../models/json.type';
import { users } from './users';
const usersModule
    // : { [k: string]: JsonObjectType | JsonValueType }
    = {
    user,
    users
}
export default usersModule